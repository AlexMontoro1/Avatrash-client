export const handleSeedChange = (setSeed) => (e) => {
    setSeed(e.target.value);
  };
  
  export const handleAccessoryChange = (setAccessory) => (e) => {
    setAccessory(e.target.value);
  };
  
  export const handleBackgroundColorChange = (setBackgroundColor) => (e) => {
    setBackgroundColor(e.target.value);
  };
  
  export const handleBackgroundTypeChange = (setBackgroundType) => (e) => {
    setBackgroundType([e.target.value]);
  };
  
  export const handleAccessoriesColorChange = (setAccessoriesColor) => (e) => {
    setAccessoriesColor([e.target.value]);
  };
  
  export const handleClothesColorChange = (setClothesColor) => (e) => {
    setClothesColor([e.target.value]);
  };
  
  export const handleClothingChange = (setClothing) => (e) => {
    setClothing([e.target.value]);
  };
  
  export const handleClothingGraphicChange = (setClothingGraphic) => (e) => {
    setClothingGraphic([e.target.value]);
  };
  
  export const handleEyebrowsChange = (setEyebrows) => (e) => {
    setEyebrows([e.target.value]);
  };
  
  export const handleEyesChange = (setEyes) => (e) => {
    setEyes([e.target.value]);
  };
  
  export const handleFacialHairChange = (setFacialHair) => (e) => {
    setFacialHair([e.target.value]);
  };
  
  export const handleFacialHairColorChange = (setFacialHairColor) => (e) => {
    setFacialHairColor([e.target.value]);
  };
  
  export const handleHairColorChange = (setHairColor) => (e) => {
    setHairColor([e.target.value]);
  };
  
  export const handleHatColorChange = (setHatColor) => (e) => {
    setHatColor([e.target.value]);
  };
  
  export const handleMouthChange = (setMouth) => (e) => {
    setMouth([e.target.value]);
  };
  
  export const handleNoseChange = (setNose) => (e) => {
    setNose([e.target.value]);
  };
  
  export const handleSkinColorChange = (setSkinColor) => (e) => {
    setSkinColor([e.target.value]);
  };
  
  export const handleStyleChange = (setStyle) => (e) => {
    setStyle([e.target.value]);
  };
  
  export const handleTopChange = (setTop) => (e) => {
    setTop([e.target.value]);
  };